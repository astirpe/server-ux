This module extends the functionality of base_tier_validation_delegation
by moving the configuration interface from the User profile to the
Employee profile.

It allows HR managers or employees to manage their Tier Validation
delegation settings (On Holiday status, dates, and replacer) directly
from the Employee form view. The settings are automatically synchronized
with the related User record.
